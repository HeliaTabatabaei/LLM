def custom_embedding(title,heading,keywords,text,segment):
    embedding_final = f""
    عنوان سند : {title}

    کلیمه های کلیدی {keywords}

    متن نهایی {text}
    ""

    return embedding_final

    pass